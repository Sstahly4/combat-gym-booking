import { GymPageMinHeightShell } from '@/components/gym/gym-page-shell'

export default function GymDetailLayout({ children }: { children: React.ReactNode }) {
  return <GymPageMinHeightShell>{children}</GymPageMinHeightShell>
}
